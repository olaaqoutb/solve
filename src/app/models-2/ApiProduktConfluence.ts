import { ApiProduktPositionConfluence } from './ApiProduktPositionConfluence';

export class ApiProduktConfluence {
  aktiv?: boolean;
  kurzbezeichnung?: string;
  langbezeichnung?: string;
  produktPosition?: ApiProduktPositionConfluence;
}
